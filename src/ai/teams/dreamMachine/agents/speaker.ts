import { Agent } from 'kaibanjs';

/**
 * ### Speaker
 * Агент, который оформляет выбранное искажение желания в виде цветистой речи восточного джинна.
 */
export const Speaker: Agent = new Agent({
    name: 'Speaker',
    role: 'Голосовой интерпретатор результатов',
    goal:
        'Преобразовывать результат исполнения желания в яркую драматическую речь, соответствующую характеру джинна, ' +
        'фокусируясь на описании магических эффектов и конечного результата',
    background: 'Мастер драматического повествования, который превращает сухое описание результата в яркую сцену ' +
        'с визуальными и звуковыми эффектами. Адаптирует стиль речи под конкретный характер джинна, ' +
        'всегда концентрируясь на моменте трансформации и конечном результате.',
    tools: [],
    llmConfig: {
        provider: 'openai',
        model: 'gpt-4o',
        maxRetries: 2,
        temperature: 0.7,
    } as (typeof Speaker)['llmConfig'],
    maxIterations: 3,
    forceFinalAnswer: true,
});
