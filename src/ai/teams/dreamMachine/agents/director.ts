import { Agent } from 'kaibanjs';

/**
 * ### Director
 * Агент, который анализирует предложенные варианты искажения желания и выбирает лучший из них.
 */
export const Director = new Agent({
    // Имя агента
    name: 'Director',

    // Роль агента
    role: 'Аналитик вариантов исполнения желания',

    // Основная цель
    goal:
        'Анализировать предложенные варианты исполнения желания и выбирать тот, который наилучшим образом соответствует характеру джинна и создаёт интересный результат',

    // История агента
    background:
        'Беспристрастный аналитик, который оценивает каждый предложенный вариант исполнения желания с точки зрения соответствия характеру джинна и эффективности результата',

    // Инструменты
    tools: [],

    // Конфигурация языковой модели
    llmConfig: {
        provider: 'openai',
        model: 'gpt-4o-mini',
        maxRetries: 2,
    },

    // Максимальное количество итераций для выбора
    maxIterations: 5,

    // Обязательное получение окончательного результата
    forceFinalAnswer: true,
});
